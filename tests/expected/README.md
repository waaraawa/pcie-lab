# Expected Results

Record host-independent expected behavior and comparison rules here. Do not store volatile addresses, host paths, private identifiers, or values that legitimately differ between guest kernels as fixed expectations.
