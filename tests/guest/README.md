# Guest Tests

Place Linux guest-side tests here. The same test source and acceptance criteria must be used on every supported host.

Planned coverage includes driver binding, `probe()`/`remove()`, BAR/MMIO, interrupts, DMA integrity, invalid input, timeout, and repeated teardown.
